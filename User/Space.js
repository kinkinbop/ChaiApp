import React from "react";
import { Text, View } from "react-native";


const SpaceScreen = () => {
    return (
        <View style={{ flex: 1, alignItems: "center", justifyContent: "center" }}>
          <Text style={{ color: "#006600", fontSize: 40 }}>Space!</Text>
        </View>
      );
};

export default SpaceScreen;